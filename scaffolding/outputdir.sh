#!/bin/bash

path=$1

mkdir -p -v "${path}"
mkdir -p -v "${path}/gen"
mkdir -p -v "${path}/gen/out"
mkdir -p -v "${path}/gen/images"
mkdir -p -v "${path}/json"
mkdir -p -v "${path}/js"
mkdir -p -v "${path}/css"
mkdir -p -v "${path}/raw"
mkdir -p -v "${path}/raw/data"
mkdir -p -v "${path}/topics"
mkdir -p -v "${path}/hashtags"
mkdir -p -v "${path}/images"
mkdir -p -v "${path}/videos"
mkdir -p -v "${path}/docs"
